# CLAUDE.md — 북극성과 시행착오 노트

격주 발행 뉴스레터의 정적 사이트다. **빌드 도구·프레임워크·npm 없음** — HTML/CSS 파일이 전부이며, 수정하면 그대로 결과물이다. Cloudflare Pages로 배포되고 `functions/`가 구독 API를 담당한다.

## 디자인 정본 (우선순위 순)

1. **`design.md`** — 디자인 철학과 규율의 원전 (Raus 스타일 레퍼런스). 새 요소를 만들기 전에 반드시 읽는다.
2. **`assets/style.css`의 `:root` 토큰** — 실제 값의 단일 출처. 여기 없는 색·크기를 새로 만들지 않는다.
3. **`/guide/`** — 라이브 스타일 가이드 (스와치·타이포·컴포넌트 실물). 로컬 서버 띄우고 눈으로 확인.

기존 클래스를 재사용하는 것이 원칙이다. 새 색·새 폰트·새 컴포넌트를 만들지 말고, 있는 것으로 조립한다.

## 철칙 (위반 = 리뷰 반려)

**색** — `:root`의 7색 토큰(`--charcoal` `--paper` `--snow` `--pine` `--marigold` `--morning-sky` `--ember`)과 그 파생 rgba 변수만 사용한다. hex 직접 기입 금지.
- **Pine**(#006434): 워드마크, 인라인 링크, 아티클 킥커(eyebrow), role 라벨만. 버튼 배경·장식 도형 금지.
- **Marigold**(#fcbd1c): 구독 카드 서피스, "곧 공개" 칩, `strong` 형광펜 하이라이터만. 텍스트 색 금지.
- **Morning Sky**: 홈 공지 바 전용. **Ember**: 공지 바 CTA의 밑줄 색 전용 (텍스트 색 금지 — 대비 미달).
- 텍스트는 charcoal 하나다. 위계는 `--muted`(.64) / `--paint`(.45) 불투명도로.

**폰트** — `Ria Sans`(--font-logo)는 **로고(워드마크) 전용**이다. 제목·본문·버튼 등 나머지 전부 Pretendard(--font-display/--font-body).
- 제목 굵기: 대형 디스플레이 **300** / 중형 400 / 소형 카드 500. **대형 제목을 700~800으로 두껍게 만들지 않는다** — 얇게 속삭이는 게 이 사이트의 목소리다.
- 폰트 파일(`assets/fonts/`)은 워드마크 글리프만 남긴 서브셋(10KB)이다. 워드마크 문구를 바꾸면 재서브셋이 필요하다.
- 전역 `word-break:keep-all`. 한글 디스플레이 행간은 1.14 아래로 내리지 않는다(글리프 잘림).

**형태** — 그림자 전면 금지. 그라데이션 전면 금지. 깊이는 snow-on-paper 레이어링과 radius로만. 모서리: 칩 12 / 카드·버튼 20 / 히어로·구독 카드 ~40 / 필 99. 0~8px 각진 모서리 금지.

**정렬** — 전부 왼쪽 정렬. 가운데 정렬 요소는 이 사이트에 하나도 없고, 앞으로도 없다.

**여백** — 섹션 경계당 헤어라인은 **한 줄**(섹션 헤더 `.shead`의 border-top만). 닫는 선을 추가로 긋지 않는다. 섹션 간격은 여백이 담당한다(~80–120px). 카피를 줄이면 빈 공간은 여백으로 흡수한다 — 뭔가로 채우지 않는다.

**모바일 퍼스트** — CSS는 모바일이 베이스, `@media(min-width:720px/900px/1024px)`로 확장. 새 요소는 390px에서 가로 스크롤 0, 터치 타깃 44px+, 인풋 16px+(iOS 줌 방지)를 지켜야 한다. 이미지는 모바일에서 컬럼 폭, 데스크톱에서 글 옆 배치가 기본 문법.

## 카피 보이스

담담하고 구체적으로(도구·비용·횟수), 옆자리에서 말하듯. 포모·과장·구루 화법·이모지 금지. 중복 문구는 걷어낸다(칩이 "곧 공개"라고 말하면 본문 메타에 또 쓰지 않는다). 상세 규칙은 README "목소리 규칙".

## 새 이슈(글) 추가하는 법

1. `templates/issue.html`을 복사해 `issues/<slug>/index.html`로 저장
2. 킥커·제목·데크·본문·글쓴이·이전/다음 링크 채우기 (구조와 클래스는 템플릿 그대로)
3. 홈(`index.html`)과 발행 목록(`issues/index.html`)에 카드 추가, `№` 부여
4. head의 OG 메타(og:title/description/url) 채우기 — og:url은 절대 URL
5. 검증(아래) 후 PR

"곧 공개" 게이트가 필요하면 기존 이슈 폴더 패턴을 따른다: `index.html`(게이트) + `full.html`(본문), 공개 시 스왑.

## 불가침 (건드리면 기능이 깨진다)

- `functions/`, `schema.sql`, `wrangler.toml` — 구독 백엔드
- 구독 폼: `#subscribe` 섹션, `#subForm`, 인라인 JS(fetch `/api/subscribe` → mailto 폴백, gtag 이벤트, `.sent`/`.done` 토글) — 스타일만 바꾸고 로직·ID는 유지
- `assets/analytics.js` 클래스 계약: 헤더 `.nav`, 읽기 링크 `read`, 밑줄 링크 `link-underline`, 구독 유도는 `#subscribe` 해시 앵커 — 이 클래스명을 지우거나 바꾸지 않는다
- 모든 페이지의 gtag 스니펫 + `analytics.js` include

## 검증 (PR 전 필수)

```bash
python3 -m http.server 8000   # repo 루트에서
```
- 모바일 390px에서 확인 (데스크톱 브라우저 devtools 또는 실기기). 가로 스크롤이 생기면 실패.
- 링크는 전부 상대 경로 — 페이지 깊이(`./` `../` `../../`)에 맞는지 클릭으로 확인.
- `grep -rn "#[0-9a-fA-F]\{6\}" <수정한 파일>` — 새 hex가 나오면 토큰으로 교체.

git 커밋은 수정한 파일만 명시해서 add 한다 (`git add -A` 금지 — 다른 작업과 섞인다).
